#include <iostream>
#include <vector>
#include <string>
#include <unordered_map>
#include <algorithm>

using namespace std;

int main() {
    int A;
    cin >> A;

    unordered_map<char, int> count;
    for (int i = 0; i < A; i++) {
        char c;
        int v;
        cin >> c >> v;
        count[c] = v;
    }








}